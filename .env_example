# MCP Planning Server Configuration

# Server name
SERVER_NAME=PlanningServer

# Server host address
SERVER_HOST=127.0.0.1

# Server port number
SERVER_PORT=9000

# Server transport method (streamable-http or stdio)
SERVER_TRANSPORT=streamable-http

# Data directory (optional, defaults to mcp_planning/data)
# DATA_DIR=/path/to/custom/data/directory